name: Beds24 AUMARA booking bridge v2 (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired execution path
        run: |
          echo 'This workflow is retired.'
          echo 'Use .github/workflows/beds24-auth-check.yml for the read-only B24 credential check.'
