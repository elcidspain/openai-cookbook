# LANDED — do not redo this work

Objective: land the already-proven Beds24 + ACP + domain-lock results on
`main` so later executions cannot treat GREEN work as missing.
Status: complete after merge of PR #166.
Scope: this branch's unique files plus this ledger. No Beds24 live writes.
No production deploys. No `aumara.me` attach to the cookbook Vercel project.
Evidence: Actions `35383953766`; sibling PRs #159 #160 #163 #165 already on
`main`; live ACP/OAuth/homepage probes; tests listed below.
Stop condition: `origin/main` contains this ledger and the unique files.
Recovery point: `main` of `elcidspain/openai-cookbook` after PR #166 merge.
Source branch: `cursor/beds24-booking-channel-rates-d78b`.

If a surface is GREEN below, do not reopen it as if it failed.

## Unique files that must stay on main

These are the files this land adds or unions on top of `771080b4`:

- `aumara-control-tower/scripts/beds24_open_booking_availability.py`
- `aumara-control-tower/scripts/tests/test_beds24_open_booking_availability.py`
- `aumara-control-tower/scripts/tests/test_aumara_acp_discovery.py`
- `aumara-control-tower/docs/BEDS24_BOOKING_RATES_AVAIL_MAP.md`
- `aumara-control-tower/systems/beds24-continuity.md`
- `elcid-site/.well-known/aumara-acp.json`
- `AUMARA_DOMAIN_LOCK.md`
- `aumara-control-tower/checkpoints/2026-09-18-beds24-booking-channel-rates.md`
- `aumara-control-tower/checkpoints/2026-09-19-merge-with-executions.md`
- `aumara-control-tower/checkpoints/2026-09-20-goal-confirmed-deploy-held.md`
- `aumara-control-tower/checkpoints/LANDED-2026-09-20.md`

Already on `main` from sibling lands (do not reimplement):

- `.github/workflows/beds24-open-booking-channel-rates.yml`
- `aumara-control-tower/scripts/beds24_open_booking_channel_rates.py`
- `aumara-control-tower/scripts/beds24_v1_booking_rates.py`
- `aumara-control-tower/docs/BEDS24_V1_CHANNEL_RATES.md`
- `.github/workflows/beds24-invite-scopes.yml`

## GREEN — already done

| Result | Where | Proof |
| --- | --- | --- |
| Fully flexible + Weekly opened in Beds24 | V1 JSON on `main` | Actions run `35383953766`, `status=SUCCESS`, `opens=["fully_flexible","weekly"]` |
| Fully flexible mapped to Booking.com | rate `66887702` | same evidence artifact |
| Inventory window through 2028-12-31 | `beds24_open_booking_availability.py` | unit tests + `[open-availability]` path |
| One-invite UI path includes `channels` / `read:channels` / `write:channels` | `beds24-continuity.md`, MAP | tests assert the strings |
| V1-first is the live rate-plan path | `beds24-open-booking-channel-rates.yml` | Production keys, `[open-channel-rates]` |
| Sibling executions on main | PRs #159 #160 #163 #165 | ancestors of this land |
| Apex OAuth `resource` matches scan host | live `aumara.me` | `GET /.well-known/oauth-protected-resource` → `resource=https://aumara.me` |
| ACP discovery public | live `aumara.me` | `GET /.well-known/acp.json` → 200, `protocol.name=acp`, human-mediated only |
| Homepage intact | live | `aumara.me` and `www` → 200 |
| Domain lock | `AUMARA_DOMAIN_LOCK.md` | `aumara.me` on `aumara-path-cut` only; cookbook must not attach it |

## HOLD

- Do not retry path-cut ERROR deploy `dpl_H56VjbNFFC7in8fw8EQJpbpLs41x`.
- Serving READY is `dpl_6o4qWVmaQCVy5Yyth2Zs3QH7rniE`.
- One Vercel route on path-cut: ACP rewrite only (`9e36be73-6fab-4d6e-8b5d-264871a0d3bb`).
- Do not fire Beds24 live writes unless the task names `[open-availability]` or `[open-channel-rates]`.
- Never use `BEDS24_PASSWORD` / `BEDS24_USERNAME`. Auth remains API-only:
  `BEDS24_REFRESH_CREDENTIAL` for v2; `BEDS24_API_KEY` + `BEDS24_PROP_KEY` for V1.

## NOT GREEN — do not pretend these are missing inventory work

| Leftover | Why it is leftover | Next action if authorized |
| --- | --- | --- |
| V2 `/channels/settings` | token still lacks `read:channels` + `write:channels` | one Marketplace invite, then dispatch channel-rates |
| Weekly as a distinct Booking.com plan | V1 Weekly has no `bookingcomRateCode` | map the Booking rate id or invite channels; do not invent a code |
| Blocked Vercel checks `aumara-www-probe` / `elcid-evento-6agosto` | unrelated projects, not this land | ignore for cookbook merge |

## Tests

```
python3 -m unittest \
  aumara-control-tower.scripts.tests.test_beds24_open_booking_availability \
  aumara-control-tower.scripts.tests.test_aumara_acp_discovery \
  aumara-control-tower.scripts.tests.test_beds24_v1_booking_rates \
  aumara-control-tower.scripts.tests.test_beds24_open_booking_channel_rates
```

Expected: all pass. Do not dispatch live Beds24 jobs to "reconfirm."
