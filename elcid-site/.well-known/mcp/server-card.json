{
  "$schema": "https://static.modelcontextprotocol.io/schemas/mcp-server-card/v1.json",
  "version": "1.0",
  "protocolVersion": "2025-06-18",
  "serverInfo": {
    "name": "elcid-public-mcp",
    "title": "EL CID Country Club Public MCP",
    "version": "1.1.0"
  },
  "description": "Read-only public EL CID guidance and booking-intent routing for the Benidoleig, Alicante property, with explicit entity and stale-data guards.",
  "transport": {
    "type": "streamable-http",
    "endpoint": "/mcp"
  },
  "authentication": {
    "required": false
  },
  "tools": [
    {
      "name": "elcid_guest_guide",
      "title": "EL CID Country Club guest guide",
      "description": "Return current public EL CID identity, stay, restaurant, contact and policy guidance with entity-disambiguation guards. Read-only.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "maxLength": 500,
            "description": "Optional guest question used only to select relevant public guidance."
          },
          "language": {
            "type": "string",
            "enum": ["en", "es", "ru"],
            "description": "Preferred response language hint."
          }
        },
        "additionalProperties": false
      }
    },
    {
      "name": "elcid_booking_options",
      "title": "EL CID booking and contact options",
      "description": "Accept a guest booking intent and return the authoritative public booking/contact routes. This does not check live inventory or price and never creates or modifies a reservation.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "checkIn": { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" },
          "checkOut": { "type": "string", "pattern": "^\\d{4}-\\d{2}-\\d{2}$" },
          "adults": { "type": "integer", "minimum": 1, "maximum": 20 },
          "children": { "type": "integer", "minimum": 0, "maximum": 20 },
          "accommodationType": {
            "type": "string",
            "enum": ["double-terrace", "family-room", "studio-kitchen", "unspecified"]
          },
          "dining": {
            "type": "string",
            "enum": ["none", "breakfast", "dinner", "private-table", "unspecified"]
          }
        },
        "additionalProperties": false
      }
    }
  ]
}
